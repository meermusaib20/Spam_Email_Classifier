# 🧠 Spam Email Classifier (Compact Version)

This project demonstrates a **Spam Email Classifier** built using **Python**, **Scikit-learn**, and **TF-IDF Vectorization**.  
It classifies emails into **Spam (1)** or **Ham (0)** using a **Logistic Regression** model.

---

## 📊 Dataset Information

**Dataset Source:** [Kaggle - Email Spam Detection (Input Dataset)](https://www.kaggle.com/code/athirags/email-spam-detection/input)

**Filename:** `mail_data.csv`  
**Description:**  
This dataset consists of email messages labeled as either **Spam** or **Ham (Not Spam)**.

| Column Name | Description |
|--------------|-------------|
| **Category / label** | Indicates whether the message is spam or ham |
| **Message / text** | Contains the email body |

---

## ⚙️ Requirements

To run this project locally, install the required libraries:

```bash
pip install pandas numpy scikit-learn seaborn matplotlib
